const theme = {
  color: {
    border: '#d5e3f5',
    stars: '#e3c44a',
    lightGrey: '#d5e3f5',
  }
}

export default theme;


//Line spacing
//Header text
//border
//Resize the canvas when window size changes.
//Button near the bottom.
//
//And then what do I do about the rest of the site?
//If I reuse the parts then they get messed up.
//If I cut and past them and restyle then I can re-place the old ones eventually after the styles get fixed up a bit.
//
//So I will do the latter.